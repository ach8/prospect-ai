const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function fix() { const jobId = 'f7037b5d-61a4-4c32-a420-7a1a7b2863a4'; const job = await prisma.researchJob.findUnique({ where: { id: jobId }}); if (!job) return console.log('Job not found'); const domains = typeof job.blacklistedDomains === 'string' ? JSON.parse(job.blacklistedDomains) : job.blacklistedDomains; console.log('Found ' + domains.length + ' domains'); const result = await prisma.prospect.updateMany({ where: { companyDomain: { in: domains }, researchJobId: null }, data: { researchJobId: jobId }}); console.log('Updated ' + result.count + ' prospects'); await prisma.researchJob.update({ where: { id: jobId }, data: { processedCount: domains.length }}); console.log('Fixed job counts!'); } fix().finally(() => process.exit(0));
