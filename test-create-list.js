const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function test() { try { const list = await prisma.prospectList.create({ data: { name: 'test_api_post', tenantId: '5ca767cb-5c94-4929-82f0-83b8fced8644' } }); console.log('Created list:', list); const result = await prisma.prospectListEntry.createMany({ data: [{ prospectId: 'e7040626-f7b6-476a-809b-a0285f718c60', prospectListId: list.id }], skipDuplicates: true }); console.log('Created entries:', result); } catch (e) { console.error('Error:', e); } finally { await prisma.$disconnect(); } } test();
